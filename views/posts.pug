doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title NNチャット
  body
    h1 NNチャット
    a(href='/logout') ログアウト
    form(method="post" action="/posts")
      div
        textarea(name="content" colos=40 row=4)
      div
        button(tpe="submit") 送信
        
    h2 投稿一覧
    each post in posts
      p 送信日時: #{post.createdAt}
      p #{post.content}
      hr